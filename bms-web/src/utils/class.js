import Vue from 'vue'

const classType = {
  /**
   * @description:
   * @param {*} val
   * @return {*}
   */
  pointStatus(val) {
    return {

    }
  }
}

export default classType
Vue.prototype.$classType = classType
